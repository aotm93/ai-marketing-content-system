{
  "python_version": "3.11",
  "build_command": "pip install -r requirements-prod.txt && cd src/dashboard && npm install && npm run build && cd ../..",
  "start_command": "uvicorn src.api.main:app --host 0.0.0.0 --port ${PORT:-8080}",
  "install_command": "pip install -r requirements-prod.txt"
}
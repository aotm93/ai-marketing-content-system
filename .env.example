# ===========================================
# REQUIRED ENVIRONMENT VARIABLES
# ===========================================
# These MUST be set for the application to run

# Database Configuration (REQUIRED)
DATABASE_URL=postgresql://user:password@localhost:5432/ai_marketing

# Admin Authentication (REQUIRED)
ADMIN_PASSWORD=your_secure_admin_password_here

# ===========================================
# OPTIONAL ENVIRONMENT VARIABLES
# ===========================================
# These can be configured via the admin panel after deployment
# Only set these if you want to override defaults

# Admin Session Configuration (Optional - auto-generated if not set)
# ADMIN_SESSION_SECRET=auto_generated_on_startup
# ADMIN_SESSION_EXPIRE_MINUTES=1440

# Redis Configuration (Optional - defaults to localhost)
# REDIS_URL=redis://localhost:6379/0

# System Configuration (Optional)
# ENVIRONMENT=production
# LOG_LEVEL=INFO

# ===========================================
# CONFIGURE VIA ADMIN PANEL
# ===========================================
# The following should be configured through the admin panel UI:
# - AI Provider settings (API keys, base URLs, models)
# - WordPress connection (URL, username, password)
# - SEO plugin settings
# - Keyword research API settings
